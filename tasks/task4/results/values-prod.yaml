replicaCount: 1

image:
  name: booking-service
  tag: 1.0.0
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

feautures:
  featX: false

resources:

livenessProbe:
  endPoint: /ping

readinessProbe:
  endPoint: /ping

